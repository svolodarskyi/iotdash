[server]
# nginx strips /grafana/ prefix, so Grafana serves from root
# but root_url tells Grafana to generate URLs with /grafana/ prefix
root_url = %(protocol)s://%(domain)s/grafana/
serve_from_sub_path = false
http_port = 3000
