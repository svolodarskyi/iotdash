if i deploy now and destroy, how to redeploy. need step by cte[]
soak test said that we have dynamic address.. so if i redeploy then it will be using different ip i need to provide new to iot devices?
COST CONTROL IN AZURE
test alerting
demo to partner. how to onboarda company, how tio add alers, how to add devices, how chnaging metrics changign the graphs.. how t oadd more graphs.. create scenario. provisioned device has 3 metrics but then we add on device type one more metrics. how that metrics will appear in metrics config and hwo quickly we can send to device and receve show on teh graphs..
show on the phone if that's possible...
CBOR TO JSON AND JSON TO CBOR .Telegraf execd plugin — decode CBOR there, everything else stays the same. Add cbor2 dependency to a tiny Python script, Telegraf calls it for every message. Done.  
cicd pipeline how enhancements can be deployed..
show archotecture and db design..
--can we demo geolocation...

-- make graphs addition dynamic via admin.. maybe even admin setup..
--device admin into client settings setup vs devices setup/devices and metrics...
--is device type in devices/provisioning coming from device_type db table or it is hardcoded in provisioned devices...
-- i want to make fucking amazing dashboard... so its clear what is tehre and you want to see it.
--and amazing user experience
make it like saas.. schadcn style
--whats the user busienss goal...
-work out the flows...
--guides how to work withg graphana how to add graphs, odify, configure etc.. how admin can switch between orgs etc...
--how provisioning work, when no device. walk step by step is anything breaking..
--how to rpettify the graph
--can we make data persist? we preseed every time. for example admins..
--explore data model. interested in foudnational device types/metrics models..
--check if these tablesa re preseeded and how device metrics is knowm when provisioning client devices.
-- add backend authentification
-instead of devices - dashboard different name.. .. improve design.. show that dashboard popular now... fix alers - admin shoul see alers and set for a company... fith company/metrics filter?

- adaptive design... so coudl be well seen on mobile.. especially admin.
  -nav panel as well..
- notification when
- active can we somehow relate is active mean receiving sygnal... maybe otehr metric online, offline..
- as well it could be as a toggle. so client see offline deivces..
  --how to add aleerts type and how web app alerts translate into graphana elrts api
  -- modify graphana emails?
  how to access

not found page. what should it be doing
sensor status Active. how is defined. whata re the rules?
logout, pointer hand

postgres as a backend for graphana.. instead of sqllite?

email, sms?
front end tests

any ml features we can implement out of the box or .. "premium features"

maybe use MCP or agent for adding a client? what's the automation.
how long would it take
is data being lost when dockr compose down.
why we do alembic migration all teh time and seeding?
is postgres not persistent

suer seect refresh frequency!!!!

who has accees to whoch grahaha account. is organisation will direct access to graphana?
or admin in graphana will have access to all?
how to add different graphs once created in account. for same sensor

can we customize email from graphana and

how to login under different organization or i can do just under admin

how can i see the postre db which devices/alerts created.. can we hard delete alerts if they are not can we see the history of alerts, logins?

what could be complex rules of alerting

is tehre a lag between front end and backend graphana. how technically is implemented with authentification and authorization

if we moved to managed services, how hard would be rewire?

Sprint 2 alerts. how can we test this. 3. Error handling for Grafana API failures (retry, surface errors to user) 4. Alert validation (duplicate detection, threshold bounds)

how can we add alert metrics.. for example not temperature.. device can have multiple metrics collected

device wil ldefinately send multiple..

maybe when we provision device we sent metrics that it will send?
is it useful to know in advance? so then on dashboard we could choose from filtetr wcho metrics of teh divice to show info..

when device sends multiple fields.. dowe save it in json?

The MQTT topic scheme already defines downstream paths ({device_id}/to/command,
{device_id}/to/config) in both ARCHITECTURE.md and PLANNING.md, but no sprint in
the TECH-LEAD-PLAYBOOK actually implements it. It's architecture-level design
only — never scheduled.

The topic structure is ready (sensor01/to/command), and the
SKILLS-AND-FEATURES-BREAKDOWN.md mentions paho-mqtt for "backend device
commands" at Sprint 0 skill level, but there's no:

- API endpoint to send commands to devices
- Backend MQTT publisher
- Frontend UI for sending commands
- Sprint assignment in the playbook

It's a designed-but-unscheduled feature. Would you like me to add it to the
backlog?

if we create alert in graphana . can it be synced by front end?

graphana has recording rules

any rules that when user logged out graphana set to refresh manually and when user comes back whatever user selected before? (rememebr user selection)

if give user access to graphana, what do they see? just graphs or alers adn settigns?

configurable dashboards page. (from database). database will have links. the frontend page interates over links and paste them

cfg topic... "collectmetrics" : {}

poweron: true/false.

ASIS

maybe sql string save into db...

dropdown: device

variable...

dashboard one page shows

2 metrics in one graph.

UID unique identoifying
pyhton instaedd of telegraph...

--if more metrics how setup will looks like (provision only 3)

--auto -rpvisioning dashboards with graphana

--process of removing users/organizations/devices. or deactivating.. any guardrails..

--admins

For later: From backend perspective what is the best solution. If we're to send incremental . What would be the implementation.

Add geolocarion example

What is graphana variable  

- How admin sees alerts: All client alerts should be visible to a admin (alerts should be stored in db as well for that matter.. when remove should be removed as well)

optimize admin device view.. its the same view as other just no organization filter??
